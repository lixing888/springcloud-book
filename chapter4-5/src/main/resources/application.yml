spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://188.131.251.86:9060/spring-cloud?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8
    username: root
    password: root123456

  jpa:
    hibernate:
      ddl-auto: create  # 第一次建表create  后面用update
    show-sql: true

